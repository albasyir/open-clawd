import { createToolManager, createAgentManager, createModelManager, createSkillManager } from 'clawpro-agent'

export const toolManager = createToolManager()
export const agentManager = createAgentManager()
export const modelManager = createModelManager()
export const skillManager = createSkillManager()
